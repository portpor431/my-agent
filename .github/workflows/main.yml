name: SSH

on:
  workflow_dispatch:

jobs:
  ssh:
    runs-on: ubuntu-latest

    steps:
      - name: Setup tmate
        uses: mxschmitt/action-tmate@v3
        with:
          limit-access-to-actor: true   # فقط خودت (بر اساس اکانت گیت‌هابت) وصل شی
